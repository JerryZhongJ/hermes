"""In-process MCP source tools for the annotator agent (``locate``, ``fold``).

Shared helpers live in :mod:`annotator.tools.utils`; each tool keeps its own
module. Both are wired into the claude runner via ``mcp_servers``.
"""
